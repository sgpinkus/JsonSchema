{
  "type": "object",
  "definitions" : {
    "userId" : {
      "type": "integer",
      "minimum": 0,
      "exclusiveMinimum": true
    }
  },
  "properties": {
    "firstName": { "type": "string", "minLength": 2 },
    "lastName": { "type": "string", "minLength": 2 },
    "email": { "type": "string", "pattern": ".+@.+", "comment": "close enough" },
    "userId": { "$ref": "#/definitions/userId" }
  },
  "required": ["firstName", "lastName", "email", "userId"]
}
